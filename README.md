# FLOWRIDE Frontend V9.2

Customer-facing Streamlit application.

Configure Streamlit secrets with `FLOWRIDE_API_URL` and `FLOWRIDE_API_KEY`. The API key is used server-side by Streamlit and should not be exposed in browser code.

V9.2 improves validation result context by showing requested, completed and skipped stocks.
